import { members } from "@/data/members";
import Link from "next/link";
export default function MembersPage() { return <main className="page"><p className="eyebrow">LE CLUB</p><h1>Les têtes<br/><em>derrière le bruit.</em></h1><p className="page-lede">Les participants identifiés dans les consignes et les archives. Les statistiques personnelles attendent la feuille historique.</p><div className="members-grid">{members.map((member, index) => <Link href={`/membres/${member.slug}`} key={member.slug} className="member-card"><span className="member-card__number">{String(index + 1).padStart(2, "0")}</span><div className="member-card__avatar">{member.displayName.slice(0, 1)}</div><h2>{member.displayName}</h2><p>{member.role === "admin" ? "Administration" : "Membre"}</p>{member.dataStatus === "needs-review" && <small>Donnée à confirmer</small>}</Link>)}</div></main>; }
