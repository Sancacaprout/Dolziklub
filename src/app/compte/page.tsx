import { AccountPanel } from "@/components/auth/account-panel";

export const metadata = { title: "Mon compte — DOL ZIKLUB" };

export default function AccountPage() {
  return <main className="page auth-page"><AccountPanel /></main>;
}
