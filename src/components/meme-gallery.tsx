"use client";
import Image from "next/image";
import { useEffect, useState } from "react";
import type { Meme } from "@/types/meme";
export function MemeGallery({ memes }: { memes: Meme[] }) { const [active, setActive] = useState<number | null>(null); useEffect(() => { const close = (event: KeyboardEvent) => event.key === "Escape" && setActive(null); addEventListener("keydown", close); return () => removeEventListener("keydown", close); }, []); return <><div className="meme-grid">{memes.map((meme, index) => <button key={meme.id} onClick={() => setActive(index)} aria-label={meme.title}><Image src={meme.src} alt={meme.alt} width={600} height={600} sizes="(max-width: 700px) 100vw, 33vw" /></button>)}</div>{active !== null && <div className="lightbox" role="dialog" aria-modal="true" aria-label={memes[active].title}><button className="lightbox__close" onClick={() => setActive(null)}>Fermer ×</button><Image src={memes[active].src} alt={memes[active].alt} width={1200} height={900} /></div>}</>; }
